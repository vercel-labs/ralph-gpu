# Ralph-gpu Time Controls Tests

This directory tracks knowledge about adding browser tests for time controls in ralph-gpu.

## Context
Ralph-gpu has time management via `context.globals`. We need to verify that time increments, pausing works, and the frame counter increments correctly.
