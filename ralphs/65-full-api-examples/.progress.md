# Progress

## Completed
- [x] Step 1: Update Example Interface in `apps/docs/lib/examples.ts`
- [x] Step 2: Update `gradient`, `wave`, and `raymarching` examples with full API code in `apps/docs/lib/examples.ts`
- [x] Step 3: Update `ShaderPlayground.tsx` to display `example.code` in the editor
- [x] Step 4: Build and visually verify the changes

## Visual Verification Results
- `pnpm build --filter docs` passes.
- Dev server running at http://localhost:3001
- Verified `gradient` example: editor shows full API code, shader renders correctly.
- Verified `wave` example: editor shows full API code, shader renders correctly.
- Verified `raymarching` example: editor shows full API code, shader renders correctly.
- No console errors in the browser.

## Next
- None. Task complete.

## Blockers
- None.
